name = "vennstack"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
cwd = "."

[build.upload]
format = "service-worker"
dir = "dist"

# For Cloudflare Pages, we'll use the Pages configuration instead
# This file is for reference - Pages uses the dashboard or wrangler pages commands

